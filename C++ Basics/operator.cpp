#include<iostream>
using namespace std;


int main()
{

    ///arithematic operators;
    // we have -(substraction)  , +(addition) , * (multiplication), /(division) , %(modulo).

    // we also have 

    int a = 10 , b = 10; 
    int result = 0;// result variable of type integer assigned to 0 initialy.
    result = a + b;
    cout<<"a + b = "<<result<<endl;//add a and b
    result = a-b;
    cout<<"a - b = "<<result<<endl;//substract a and b
    result = a*b;
    cout<<"a * b = "<<result<<endl;//multiply a and b
    result = a/b;
    cout<<"a / b = "<<result<<endl;//divide a by b
    result = a-b;
    cout<<"a % b = "<<result<<endl;//remaider of a/b
    result = a==b;
    cout<<"a == b = "<<result<<endl;//check if a == b 

}